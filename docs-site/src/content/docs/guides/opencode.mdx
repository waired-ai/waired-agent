---
title: Use it from OpenCode
description: Point OpenCode at your own model with one command — no API key, no environment variable, and your existing default model left alone.
meta:
  audience: OpenCode users
  needs: OpenCode installed, and Waired signed in
  time: 2 minutes
---

import { Aside, CardGrid, LinkCard } from '@astrojs/starlight/components';
import Expected from '../../../components/Expected.astro';

{/* Split out of the old combined guides/coding-agents.mdx. OpenCode had four
    lines inside a page titled after Claude Code, which is not findable by
    anyone searching for OpenCode. */}

## Connect it

```sh
waired link opencode
```

That is the whole setup. Start OpenCode as usual afterwards and pick the
Waired model.

<Expected>
	```
	opencode:    configured

	OpenCode integration:
	  - Plugin written to ~/.config/opencode/plugin/waired.js
	    (registers the 'waired' provider). Restart opencode to pick it up.
	```
	OpenCode's model picker gains a **Waired** provider with one entry:

	| Model | Use it for |
	|---|---|
	| `waired/default` (shown as **Waired Default**) | Whichever model Waired is running here |
</Expected>

No API key and no environment variable are involved. Waired connects OpenCode
to the model running on your own machine directly.

<Aside type="note" title="Your own default model is not touched">
	Linking adds the Waired model to the list you can choose from. Which model
	OpenCode starts with stays whatever you had set — you switch to the Waired
	one when you want it, with `/models` or the picker.
</Aside>

<Aside type="tip" title="Have several coding tools installed?">
	`waired link` with no name sets up every one it finds — Claude Code, OpenCode
	and OpenClaw.
</Aside>

## What it changed

Three files, all inside your own home folder:

| Path | What it is |
|---|---|
| `~/.config/opencode/plugin/waired.js` | A small plugin that registers the `waired` provider |
| `~/.config/opencode/commands/waired-status.md` | The `/waired-status` command inside OpenCode |
| `~/.config/opencode/commands/waired-doctor.md` | The `/waired-doctor` command inside OpenCode |

Your `opencode.json` is not written, and nothing outside your home folder is
touched.

## Check it, or remove it

[`waired doctor`](/getting-started/doctor/) verifies the whole path — the
connection, whether routing is paused, whether a model is loaded — and repairs
what it can when you press **f**. Inside OpenCode, `/waired-status` and
`/waired-doctor` run the same checks and summarise them for you.

To remove the integration:

```sh
waired unlink opencode
```

It is surgical: it undoes what `waired link` added and nothing else. Files you
put in `~/.config/opencode/commands/` yourself, and anything OpenCode wrote
there, stay.

You can also see and re-apply it from the Waired app — click the **Waired
icon** → **Settings** → **OpenCode**.

<CardGrid>
	<LinkCard title="Use it from Claude Code" href="/guides/claude-code/" description="Plus route switching mid-session." />
	<LinkCard title="Use it from OpenClaw" href="/guides/openclaw/" description="The same, for OpenClaw." />
	<LinkCard title="Use it from a chat app" href="/guides/chat-clients/" description="Any OpenAI-compatible client." />
</CardGrid>
