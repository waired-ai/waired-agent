---
title: Waired documentation
description: Run local LLMs on your own machines and use them from anywhere — over one private, end-to-end encrypted link.
template: splash
editUrl: false
hero:
  title: Waired
  tagline: Your own AI, on your own hardware — reachable from anywhere over a private, encrypted link.
  actions:
    - text: Quickstart — 10 minutes
      link: /quickstart/
      icon: right-arrow
      variant: primary
    - text: What is Waired?
      link: /what-is-waired/
      icon: open-book
      variant: secondary
---

import { Tabs, TabItem, CardGrid, Aside } from '@astrojs/starlight/components';
import IconLinkCard from '../../components/IconLinkCard.astro';

{/* LANDING PATTERN 1f — 1e (tiles above the command), trimmed just enough
    that all four tiles are on the first screen.

    1e's order only pays off if the tiles are actually seen, so the space above
    them is what gets cut, in this order:
      - the h1, from "Waired documentation" at splash size to "Waired";
      - the hero's block padding;
      - the intro paragraph, down to one sentence;
      - the page's paragraph rhythm.
    The tiles themselves keep near-full copy and comfortable padding — the
    point is to show them, not to shrink them. Two lines came off each
    description to buy the last row of pixels. */}

<style is:global>{`
	/*
	 * Gated on body:has(#wd-tiles-fold) — the hidden marker below — so nothing
	 * here reaches another route even if the bundler merges CSS chunks.
	 * Unlayered, so it beats Starlight's @layer starlight.components without
	 * !important.
	 *
	 * Editing note: MDX parses { as an expression start inside <style> too, so
	 * this CSS is a template literal and must contain no backticks.
	 */
	body:has(#wd-tiles-fold) .hero {
		padding-block: 0.75rem;
		gap: 1.25rem;
	}
	body:has(#wd-tiles-fold) .hero h1 {
		font-size: var(--sl-text-4xl);
		line-height: 1.15;
	}
	body:has(#wd-tiles-fold) .hero .tagline {
		font-size: var(--sl-text-lg);
		max-width: 48ch;
	}
	body:has(#wd-tiles-fold) .sl-markdown-content > :first-child {
		margin-top: 1.25rem;
	}
	body:has(#wd-tiles-fold) .sl-markdown-content > * + * {
		margin-top: 1.5rem;
	}
	body:has(#wd-tiles-fold) .sl-markdown-content h2 {
		margin-top: 1.75rem;
		font-size: var(--sl-text-2xl);
	}
	body:has(#wd-tiles-fold) .card-grid {
		gap: 1rem;
	}
	body:has(#wd-tiles-fold) .wd-card {
		--wd-pad: clamp(1rem, calc(0.125rem + 1.5vw), 1.5rem);
	}
	body:has(#wd-tiles-fold) .wd-title {
		font-size: var(--sl-text-lg);
	}
`}</style>

<div id="wd-tiles-fold" hidden></div>

Type in Claude Code on any laptop and the answer comes from a machine you own —
no per-message bill, no subscription, and your prompts never leave your devices.

## Why people run it

<CardGrid>
	<IconLinkCard title="One command" icon="rocket" href="/quickstart/">
		Install with a single line, sign in with Google, and finish on screen or in
		the terminal — about ten minutes either way.
	</IconLinkCard>
	<IconLinkCard title="Free to run" icon="approve-check" href="/reference/model-catalog/">
		The model runs on hardware you already own — a recent processor is enough,
		a graphics card makes it fast. See what fits your machine.
	</IconLinkCard>
	<IconLinkCard title="Private by design" icon="padlock" href="/concepts/privacy/">
		Your devices talk directly over an end-to-end encrypted link. Waired's own
		servers never see your prompts or replies.
	</IconLinkCard>
	<IconLinkCard title="Your tools, unchanged" icon="puzzle" href="/guides/coding-agents/">
		Claude Code, OpenCode and any OpenAI-compatible client keep working. What
		changes is where the answer comes from, not how you work.
	</IconLinkCard>
</CardGrid>

## Install in one command

<Tabs syncKey="os">
	<TabItem label="Windows">
		From **Terminal (Administrator)** — right-click the Start button:

		```powershell
		iwr -useb https://github.com/waired-ai/waired-agent/releases/latest/download/install.ps1 | iex
		```
	</TabItem>
	<TabItem label="macOS">
		From **Terminal** — press ⌘ + Space and type `Terminal`:

		```sh
		curl -fsSL https://github.com/waired-ai/waired-agent/releases/latest/download/install.sh | sh
		```
	</TabItem>
	<TabItem label="Linux">
		From a terminal, on Ubuntu 24.04+ or Debian trixie+:

		```sh
		curl -fsSL https://github.com/waired-ai/waired-agent/releases/latest/download/install.sh | sh
		```
	</TabItem>
</Tabs>

It shows what it will do and asks before changing anything. Then finish setup
**on screen or in the terminal** — the [Quickstart](/quickstart/) covers both.

## Find what you need

<CardGrid>
	<IconLinkCard title="Use it from another device" icon="laptop" href="/getting-started/add-a-device/">
		Sign in on your laptop with the same account and it reaches your AI from
		anywhere — no ports to open, no VPN to configure.
	</IconLinkCard>
	<IconLinkCard title="Choose which model runs" icon="setting" href="/guides/choose-a-model/">
		A bigger model for better answers, a smaller one for speed — and how to
		switch without breaking anything.
	</IconLinkCard>
	<IconLinkCard title="Something looks wrong" icon="warning" href="/troubleshooting/">
		Start from the symptom you are seeing and get the one command that fixes
		it.
	</IconLinkCard>
	<IconLinkCard title="Every command" icon="list-format" href="/reference/cli/">
		The full `waired` reference, grouped by what you are trying to do.
	</IconLinkCard>
</CardGrid>

<Aside type="tip" title="New to all of this?">
	[What is Waired?](/what-is-waired/) explains it in plain language, with a
	picture, in about two minutes — and it is honest about what Waired *cannot*
	do.
</Aside>
