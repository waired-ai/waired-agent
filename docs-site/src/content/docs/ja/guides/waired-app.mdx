---
title: Waired アプリ
description: Waired アイコンの中身をすべて — 状態の読み方、モデルの切り替え、どのマシンが答えるかの指定、共有、サインアウト。ターミナルは不要です。
meta:
  audience: 画面のあるパソコンで Waired を使う人
  needs: Waired がインストール済みでサインイン済みであること
  time: 5 分（最初の 1 週間は開いたままにしておくと便利です）
sourceHash: 154a794958092c7a
---

import { Aside, Steps, CardGrid, LinkCard } from '@astrojs/starlight/components';
import Screenshot from '../../../../components/Screenshot.astro';
import Expected from '../../../../components/Expected.astro';

画面のあるパソコンでは、**Waired アプリがそのまま Waired 本体**です。
このサイトに書かれていることのほとんどは、コマンドを打たずにここから行えます。

## どこにあるか

**Waired アイコン**をクリックします。

| | |
|---|---|
| **macOS** | 画面上部のメニューバー |
| **Windows** | タスクバー右端、**時計のとなり**。見当たらないときは **^**（隠れているアイコンを表示）をクリック |
| **Linux** | 上部または下部バーの、時計のとなり |

<Screenshot
	src="app-ready.png"
	alt="Waired アプリのメニューが開いた状態。上部に接続状態、その下にサインイン中のアカウント、さらにモデル・共有・コーディングツールの各項目。"
	caption="このメニュー 1 枚がアプリのすべてです。"
	width="26rem"
/>

<Aside type="note" title="アイコンだけが Waired ではありません">
	Waired はパソコンの起動時に立ち上がる常駐サービスとして動いています。アイコンは
	そこへ話しかけるための窓口です。アイコンを終了しても Waired は止まりません
	（→ [終了する](#終了する)）。
</Aside>

## いちばん上の行を読む

最初の 1 行が、ほかのすべての状態を表しています。ここが **Connected** でなければ、
以下の説明どおりには動きません。

| 表示 | 意味 | すること |
|---|---|---|
| **● Connected** | サインイン済みで、到達できる状態。 | 対応不要。 |
| **◐ Connecting…** | 接続中。サインイン直後やスリープ復帰直後に出ます。 | 数秒待ちます。 |
| **○ Not signed in** | インストール済みですが、アカウントが未設定。 | 下の **Sign in…** を選びます。 |
| **◐ Signing in… / ◐ Activating…** | 開始したサインインが進行中。 | 開いたブラウザで完了させます。 |
| **○ Disconnected** | サインイン済みだが、いまネットワークに届いていない。 | **Connect** を選びます。直らなければ[うまくいかないとき](/ja/troubleshooting/)へ。 |
| **◐ Waired agent is starting…** | サインイン直後の正常な状態です。Windows では常駐サービスがアイコンより数分遅れて起動します。 | 待つか、**Start the Waired agent…** ですぐに起動できます。 |
| **⚠ Waired agent is not running** | 起動しているはずの常駐サービスが起動していません。 | **Start the Waired agent…** を選びます（パソコンが管理者の確認を求めます）。[Waired のアイコンに「エージェントが起動していません」と出る](/ja/troubleshooting/#the-waired-icon-says-the-agent-is-not-running)も参照。 |
| **◐ Reconnecting to the Waired agent…** | 常駐サービスは動いていますが、まだ応答がありません。サインインは保たれています。 | 待ってください。通常はすぐ戻ります。 |

対応が要るときは、**Connected** の代わりにその内容が出ます。

| 表示 | 意味 | すること |
|---|---|---|
| **⚠ No engine is answering** | 答えられるものが 1 つもありません。このパソコンで AI が動いておらず、ほかのパソコンにも届いていない状態です。**Waired** の項目にある Claude Code のターンは失敗してその旨を伝えます。そのセッションが Anthropic のモデルを選んでいない限り、クラウドへは何も行きません。 | ここで AI を動かす（**Inference → Start inference engine**）か、AI が動いているパソコンの電源を入れます。 |
| **⚠ Claude Code not served (…)** | 常駐サービスが停止または一時停止しているため、Waired は何も答えていません。 | サービスの状態は[うまくいかないとき](/ja/troubleshooting/)を参照。 |
| **⚠ OpenCode integration needs attention** / **⚠ OpenClaw integration needs attention** | そのツールの設定ファイルが Waired を指さなくなっています。 | **Settings** で、名前の出ているツールの **Reconfigure…** を選びます。 |
| **⚠ Inference fell back recently** | 直近 10 分に、指定どおりに処理できなかったリクエストがありました。 | **Settings → Recent activity** に一覧が出ます。 |

同じ 1 行が、製品名を前に付けた形でアイコンのツールチップにもなります。
メニューを開かなくても、アイコンにカーソルを合わせれば読めます。

## いま動いているもの

接続の項目の下に 3 行あります。「ちゃんと動いているか」への答えで、
どれもサブメニューを開かずに読めます。

| 表示 | 意味 |
|---|---|
| **● Engine: ready — Qwen3 8B Instruct** | このパソコンがその AI を動かしていて、リクエストに答えます。 |
| **● Engine: ready — Qwen3 8B Instruct (not loaded)** | 同じですが、いま AI がメモリに載っていません。次のリクエストは読み込みを待ちます（ふつう 20 秒〜1 分）。その後は **Keep-alive** の設定の時間だけ載ったままになります。 |
| **◐ Engine: downloading — …** | モデルのファイルをネットワークから取得中です。 |
| **○ Engine: off on this computer** | このパソコンではローカル推論を切っています。リクエストはほかのパソコンへ回ります。 |
| **○ Engine: none on this computer** | このパソコンには推論エンジンが入っていません。リクエストを送るだけに使うパソコンでは正常な状態です。 |
| **○ Engine: stopped (memory freed)** | メモリを空けるためにエンジンを止めた状態です。**Inference → Start inference engine** で戻せます。 |
| **⚠ Engine: engine failed** / **⚠ Engine: pull failed** | エンジンが起動できなかった、またはダウンロードに失敗しました。理由は **Inference** にあります。 |
| **● Peers: 2 of 4 serving** | 自分のほかのパソコンのうち、いまこのパソコンのリクエストに答えられるのが何台か。 |
| **● Claude Code: routed through Waired** | このパソコンの Claude Code が、リクエストをここへ送っています。 |
| **○ Claude Code: not routed through Waired** | 送っていません（Anthropic API に直接つながっています）。`waired claude enable` で切り替えられます。 |
| **⚠ Claude Code: routed elsewhere (…)** | このパソコンで、ほかの何かが `ANTHROPIC_BASE_URL` を設定しています。Waired はそれを上書きしません。 |

常駐サービスが古くてその情報を返せないときは、その行は出ません。

**どの行もクリックできます。** 状態を伝える行は — サブメニューの中のものも含めて —
クリックすると下の **Status** が開きます。

## アカウントの行

いちばん上の行の下に、サインインしているアカウントが出ます。クリックすると
[Web コンソール](/ja/guides/web-console/)のそのアカウントのページ
（表示名・アカウント ID・現在のセッション）が開きます。

## サインインと接続

状態表示のすぐ下の項目は、状況に応じて変わります。

- **Sign in…** — サインインを開始します。Google アカウント選択のためブラウザが開きます。
- **Disconnect** — サインアウトせずに、このパソコンをプライベートネットワークから
  外します。ルーティングが止まるので、しばらくクラウドに戻したいときに使えます。
- **Connect** — 戻します。

<Aside type="caution" title="ここからのサインインだけでは初回セットアップは終わりません">
	新品のパソコンでは、推論エンジンのインストールに管理者権限が必要で、
	アプリ側からはそれを取得できません。未セットアップのパソコンでアイコンから
	サインインすると、セットアップは途中で止まり、ターミナルで `waired init` を
	実行するよう促されます。初回は[クイックスタート](/ja/quickstart/)に従ってください
	（インストーラが一気に通してくれます）。
</Aside>

## Models — 動かす AI を変える

いま動いているモデルは上の **Engine** の行に出ます。**Models** サブメニューには
このパソコンで動かせるモデルが並び、選ぶと切り替わります。

切り替えは**その場で反映**されます（数秒で読み込み直され、その間もサービスは継続）。
未ダウンロードのモデルを選んだ場合はダウンロードが始まり、完了するまでは
これまでのモデルが応答を続けます。

大きくすべきか小さくすべきかの判断は
[使うモデルを選ぶ](/ja/guides/choose-a-model/)にあります。

## Inference — 推論エンジンそのもの

**Inference** には、**このパソコンで動く** AI の操作がまとまっています。
リクエストの**行き先**は、ひとつ下の **Inference routing** です。

| 項目 | 内容 |
|---|---|
| **Pause / Resume local inference** | このパソコンが AI リクエストに応答するかどうか。停止中でも**ほかの**パソコンの AI は使えます。モデルは**載ったまま**なので再開は一瞬です。 |
| **Run models on this computer** | 同じ切り替えを、まだ一度もモデルを動かしていないパソコン向けに言い換えたもの。リクエストに現実的な時間で答えられないと計測されたマシンでは、Waired はこの状態から始まります。選ぶと推論エンジンとモデルをあわせて導入します。 |
| **Start / Stop inference engine** | 設定を変えずに、いますぐエンジンを止めてメモリ（数 GB）を解放します。再開するとモデルを読み直します。 |
| **Install Ollama…** | 推論エンジンが未導入のときに表示されます。 |
| **Share this computer / Stop sharing this computer** | このパソコンをそもそも貸し出すかどうか — 自分のほかのパソコンにも、公開のゲストにも共通の 1 つのスイッチ。オフでも自分では使えます。下の **Sharing:** 行が結果を報告します（次の行を参照）。 |
| **Sharing: enabled / disabled / paused / nobody, set in the console** | 実際に起きていること。**enabled** — ほかへ提供中。**disabled** — 自分でオフにした。**paused** — このセッションの間だけ提供を保留（通常はアプリ起動時に解除）。**nobody, set in the console** — スイッチはオンだが、コンソールがどの提供先からも外している。*誰に*提供するかは、ここではなく[ウェブコンソール](/ja/guides/web-console/)で設定します。 |
| **Engine: ready / downloading / disabled** | エンジンの状態。**downloading** はモデルのファイルをネットワークから取得中という意味で、メモリに載っているかどうか（次の行）とは別の話です。 |
| **Model: …** | 使っているモデルと、いまメモリに載っているか（**(loaded)** / **(not loaded)**）。載っていればすぐ答えが始まり、載っていなければ読み直してから答えます。処理中はリクエスト数も付きます — **(loaded, serving 2 requests)**。 |
| **Unload model (free memory)** | エンジンは動かしたままモデルのメモリだけを返します。このパソコンは応答を続け、次の質問でモデルを読み直します。返すものが無いときは **Model not loaded** と表示され、選べません。 |
| **Keep-alive: …** | 最後の質問からモデルをメモリに保持する時間。下に **Always** / **15 minutes** / **1 hour** / **8 hours** が並びます。 |

<Aside type="note" title="この 2 行が出ないパソコンもあります">
	推論エンジンが動いている間ずっとモデルを保持するパソコンでは、単独で降ろす
	ものが無く、設定できる時間もありません。押しても何も起きない行を出すより、
	**Unload model** と **Keep-alive** を出さないようにしています。
	そこでメモリを解放する手段は **Stop inference engine** で、モデルの分だけ
	でなくエンジンが確保している分もまとめて返ります。
</Aside>

<Aside type="note" title="Pause と Unload と Stop は別のものです">
	**Pause local inference** は応答を止めるだけで、モデルはメモリに載ったまま
	（再開のコストがゼロ）です。**Unload model** はメモリを返しつつ応答は続けます。
	**Stop inference engine** はその両方（メモリも保持せず、ここでは何も答えない）です。
	数 GB を空けたうえで作業を続けたいなら Unload を選んでください。
</Aside>

<Aside type="tip" title="モデルを載せたままにする理由">
	Waired は一度読み込んだモデルをメモリに保持します。読み直すには、マシンと
	モデルによって約 17 秒から 1 分ほど、最初のトークンが出るまでに余分にかかり、
	その大半は裏で読み直しておいても取り戻せません。そのため既定は **Always**
	です。自動でメモリを返してほしい場合は時間を選んでください。同じ設定は
	コマンドラインの `waired inference residency` でも変更できます。
</Aside>

## Inference routing — どのマシンが答えるか

**Inference routing** は、このパソコンで何か尋ねたときに**どのマシンが答えるか**を
決めます。メニューの先頭に、いまの答え（**Worker:**）と、ほかのパソコンにいま
届くかどうかが出ます。

**Choose automatically** の下では、選んだルールに従って Waired が自動で選びます。

| 選択肢 | 意味 |
|---|---|
| **Auto** | 自前の AI があればそれを使い、なければ自分のほかのパソコンへ。既定値です。 |
| **Local only** | ほかのパソコンは決して使いません。 |
| **Peer preferred** | 自分でも答えられる場合でも、ほかのパソコンを優先します。高速なデスクトップの隣にあるノートパソコンで有効です。 |
| **Peer only** | ほかのパソコンだけを使います。どれも答えられないときは、黙ってここで動かさずにエラーになります。このマシンを空けておきたいときに選びます。 |

**Pin to one peer** の下では、ルールを飛ばして常に特定の 1 台を使えます。一覧から
選び、**(clear pin)** で **Auto** に戻ります。各項目にはパソコン名と、そこで動いて
いるモデルが並びます。どちらからでも選べます。

いま応答できないパソコンは、モデル名の横に理由が出ます — モデルを取得中なら
*(downloading)*、ダウンロードに失敗したなら *(pull failed)*、推論エンジンが落ちて
いるなら *(engine failed)*。理由を答えられないパソコンや、しばらく連絡が取れて
いないパソコンは *(unavailable)* です。いずれの場合も指定はそのまま残り、その 1 台が
戻るまで応答できないだけです。

<Aside type="tip" title="メモリが足りないときは「エンジンを止める」">
	ほかの作業でメモリが苦しいときは **Stop inference engine** で即座に解放できます。
	壊れるものは何もありません — リクエストはほかのパソコンへ回るか、再開するまで待つだけです。
</Aside>

## Public computers — ほかの人の空きマシンを使う

**Public computers** にあるのは **Use public computers** — ほかの Waired
ユーザーが貸し出しているマシンで自分の処理を走らせるかどうかです。
*自分のより速いときは自動で使う* / *指定したときだけ* / *使わない* から
選びます。自分でオンにしない限りオフです。

自分のパソコンを公開で共有する操作はこのメニューにはありません。オン/オフは
[ウェブコンソール](/ja/guides/web-console/)で切り替え、上の共有スイッチを
オフにすればほかの提供と一緒に止まります。

オンにする前に[パブリック共有](/ja/public-share/)を読んでください。要点は
**公開マシンの持ち主は、あなたが送った内容を読める**ということです。機密は送らないでください。
サブメニューの **Privacy & safety…** がそのページを開きます。

## Claude Code — Waired を向いているか

このサブメニューは、このパソコンの Claude Code が Waired を向いているかどうかを
示します。向いていれば **Claude integration: ● active** と
**Claude: ✓ routed to local gateway**。向いていなければ、有効にするコマンドを
書いた **ⓘ not active yet — …** の行が出ます。

各ターンの実行先はここでは選びません。Claude Code の `/model` で選んだモデル —
自分のパソコンなら **Waired** の項目、クラウドなら Anthropic のモデル — が決め、
Waired がターンを勝手に反対側へ移すことはありません。
詳細は [Claude Code から使う](/ja/guides/claude-code/)。

## This device

このパソコンのネットワーク上の名前、Waired アドレス（クリックでコピー）、
所属ネットワーク、そして自分のほかのパソコンが 1 台 1 行で並びます。

| 表示 | 意味 |
|---|---|
| **● sv-evox2 — qwen3.6-35b-a3b** | そのパソコンはこのパソコンのリクエストに答えられます。動かしているモデルが後ろに出ます。 |
| **○ sv-xps15 — no engine** | 答えられません。理由が後ろに出ます。ほかに **unavailable**（最近報告が届いていない）、**awaiting model**、**downloading**、**pull failed**、**engine failed** があります。 |

ほかの人が公開で貸してくれているパソコンは、その共有が与えた名前で出ます
（そのパソコン自身の名前は出ません）。

この行をクリックすると [Status](#status) が開き、同じパソコンについて、
ハードウェア、このパソコンからの届き方、最後に報告が来た時刻まで出ます。

## Status…

いま Waired が把握していることが 1 つの窓にまとまって出ます。このパソコンの
エンジン・モデル・共有・ルーティング、Claude Code がここを向いているか、
ほかのパソコンそれぞれが何をどのハードウェアで動かしているか、そして直近の
フォールバック。状態を伝える行をクリックすると開くのはこれで、**Status…** の行は
それを知らない人のための入口です。

**Copy details** を押すと、より詳しい版がクリップボードに入ります。全パソコン分に
加えて、識別子・アドレス・接続の種類・時間など、問い合わせで次に訊かれるものが
入っています。見えているものを打ち直さずに、そのまま issue に貼ってください。
**Close** を押した場合はクリップボードに触れません。

窓に出るのは数秒前に取った最新の読み取りで、それがいつのものかは上の行に出ます。
取り直したいときはもう一度開いてください。

<Aside type="note" title="グレーの行は「できない」という意味です">
	状態を伝える行はグレーになりません。これは意図的です — どの OS でも、
	メニューのグレーは「いまは選べない」を意味します。ですから
	**Model not loaded** のようにグレーの行を見かけたときは、それは
	「そのことがいまはできない」と言っているのであって、飾りではありません。
</Aside>

## Open Admin Console…

[Web コンソール](/ja/guides/web-console/)を開きます。端末一覧、セットアップ画面、
パブリック共有の設定、アカウントを、どのブラウザからでも扱えます。

## Settings

| 項目 | 内容 |
|---|---|
| **OpenCode / OpenClaw** | 各コーディングツールが Waired を使う設定になっているか、設定ファイルの場所。これらのファイルは**あなたの**ホームフォルダにあり、アプリはバックグラウンドサービスとしてではなく、あなたとして読み書きします。**Reconfigure** は確認のうえ `waired link opencode`（または `openclaw`）を実行します。 |
| **Recent activity** | 直近 10 分のうち、指定どおりに応答できず別のところが答えた回数。空なのが正常です。各行は `<モデル> — <元> → <先> (<理由>, <経過時間>)` の形で、理由は **engine not ready** / **sharing off** / **at capacity** / **legacy peer** / **auth error** / **transport error** / **paused** のいずれかです。 |
| **About Waired** | バージョン情報。 |
| **Start Waired on login** | パソコンにサインインしたときアイコンを表示するかどうか。チェックが入っていれば表示します。オフにしても常駐サービスは止まりません。 |
| **Sign out…** | このパソコンをサインアウトし、識別情報を削除します。ネットワークから外れ、次に使うときは最初からサインインし直しになります。 |

<Aside type="caution" title="Sign out… と Disconnect は別物です">
	**Disconnect** は一時的で、ワンクリックで戻せます。**Sign out…** は識別情報の削除で、
	次にサインインすると**新しい端末として登録**されます。しばらく自分のモデルを使わないだけなら
	**Disconnect** か、[ルーティングの一時停止](/ja/guides/pause/)を使ってください。
</Aside>

## アップデートがあるとき

メニュー上部にインストールを促す行が現れます。クリックすると管理者権限を求めたうえで
その場で更新します（→ [Waired を更新する](/ja/getting-started/update/)）。
更新が終わると、アプリは新しいバージョンで自動的に開き直されます。

**Notify me about updates** はポップアップ通知だけを止めます。メニュー内の行は
どちらの設定でも残るので、更新できなくなることはありません。中断されなくなるだけです。

## 終了する

**Quit** はアイコンを閉じると同時に、このパソコンを片付けます。推論エンジンを止めて
メモリを返し、誰も操作していない間はこのセッションのすべての共有が止まります —
自分のほかのパソコンからも、公開のゲストからも、処理は回ってきません。
Waired 自体はバックグラウンドで動き続け、コーディングツールもそのまま使えます
（ほかのパソコンか、クラウドに回ります）。パソコンからサインアウトしたときや
シャットダウンしたときも **Quit** と同じことが起きます — どちらも操作する人が
いなくなるので、推論エンジンを止め、誰にも処理が回ってこないようにします。

設定は何も失われません。共有の設定はそのままなので、アイコンを起動し直せば
このパソコンは元どおり AI を提供します。共有をオフにしていたなら、オフのままです。

アイコンを戻すには、アプリ一覧から Waired を起動するか、
（**Start Waired on login** にチェックがあれば）パソコンにサインインし直します。

<Expected tone="bad" title="アイコンがまったく見当たらない">
	Waired は入っているのにアイコンが出ていない状態です。Windows ではまず **^**
	（隠れているアイコン）を確認してください。それ以外は、アプリ一覧から Waired アプリを
	起動します。Mac なら Launchpad の **Waired**、または Spotlight に「Waired」と
	入力します。Linux で、時計のとなりにアイコンを出す仕組みがないデスクトップでは
	拡張機能が必要です → [Linux にインストールする](/ja/getting-started/install/linux/)

	いずれの場合も常駐サービスには影響がありません。ターミナルで `waired status` を実行すれば、
	正常に動いていることが確認できます。
</Expected>

<CardGrid>
	<LinkCard title="使うモデルを選ぶ" href="/ja/guides/choose-a-model/" description="品質重視なら大きく、速度重視なら小さく。壊さずに切り替える。" />
	<LinkCard title="Web コンソール" href="/ja/guides/web-console/" description="同じ操作をどのブラウザからでも。端末一覧つき。" />
	<LinkCard title="状態を診断する" href="/ja/getting-started/doctor/" description="全体を検査して、多くをその場で修復する 1 コマンド。" />
	<LinkCard title="コマンド一覧" href="/ja/reference/cli/" description="このページの内容を、ターミナルから行う方法。" />
</CardGrid>
