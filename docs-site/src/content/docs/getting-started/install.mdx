---
title: Install
description: Choose how to install Waired on Windows, macOS, or Linux — and what the installer actually puts on your computer.
---

import { Aside, CardGrid, LinkCard } from '@astrojs/starlight/components';

{/* Journey-first IA. This page used to be 254 lines covering install, update,
    uninstall, clean reinstall, the edge channel and the Ollama opt-out. It is
    now a hub: pick your OS. Update and Uninstall are their own pages. */}

Waired installs in one step on all three platforms. Pick yours:

<CardGrid>
	<LinkCard title="Windows" href="/getting-started/install/windows/" description="Double-click installer, or one PowerShell line." />
	<LinkCard title="macOS" href="/getting-started/install/macos/" description="One Terminal line. Intel and Apple Silicon." />
	<LinkCard title="Linux" href="/getting-started/install/linux/" description="One Terminal line. Ubuntu 24.04+ and Debian trixie+." />
</CardGrid>

If you just want to get going, follow the [Quickstart](/quickstart/) instead —
it covers installing *and* setting up in one pass.

## Will it run on my computer?

| Role | What works |
|---|---|
| **Runs the AI** | Ubuntu 24.04+, Debian trixie+, Windows 11 / Server 2025, macOS 13+ (Apple Silicon recommended) |
| **Uses the AI** | Any of the above — no graphics card needed |

A graphics card makes answers much faster, but a recent processor runs a small
model fine. The computer you *type* on needs nothing special.

How much memory a given model needs is in the
[model catalog](/reference/model-catalog/) — though you do not have to read it,
because Waired picks a model that fits when you set up.

## What the installer puts on your computer

- **The Waired app** — a background service that keeps this computer connected,
  and a `waired` command you can type. The service starts with the computer,
  before anyone logs in.
- **An icon** in your menu bar (macOS) or system tray (Windows, Linux) for
  everyday controls.
- **Nothing else, yet.** The AI software itself (a multi-gigabyte download) is
  installed later, during setup, and only if you say this computer should run
  models.

Every installer prints a summary of what it is about to do and waits for you to
confirm before touching anything.

<Aside type="note" title="Already run Ollama?">
	Waired can leave your existing installation alone. See
	[Advanced install options](/reference/install-options/).
</Aside>

## After installing

<CardGrid>
	<LinkCard title="Sign in and set up" href="/getting-started/first-run/" description="One command starts it; the rest happens in your browser." />
	<LinkCard title="Update Waired" href="/getting-started/update/" description="Check for and apply updates, and switch release channels." />
	<LinkCard title="Uninstall" href="/getting-started/uninstall/" description="Remove it, with or without your downloaded models." />
	<LinkCard title="Advanced install options" href="/reference/install-options/" description="Pin a version, choose a location, target a different service." />
</CardGrid>
